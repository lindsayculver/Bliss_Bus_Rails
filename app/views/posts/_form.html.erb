<%= render('layouts/errors', :object => @post) %>

<!-- <%= form_for [@bus, @post] do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :text %>
<%= f.text_area :text %>
<p><%= f.submit %></p>
<% end %> -->

<%= simple_form_for [@bus, @post] do |f| %>
  <%= f.input :name %>
  <%= f.input :text %>
  <%= f.button :submit %>
<% end %>
